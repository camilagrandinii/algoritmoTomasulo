var floatingRegisterList = []
var instructionUnit = []

var addSubReservation = ReservationStation(2, 2, "Adder")
var multDivReservation = ReservationStation(4, 3, "Multiplier")
var LoadStoreReservation = ReservationStation(6, 1, "Memory")

function Run(){
    
}